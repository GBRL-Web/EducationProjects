<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="../public/style.css" />
    <title>Hotel EUREKA!</title>
  </head>

  <body>
    <header id="header">
      <% if (loginS == true) { %> <% if (loginP == true) { %> <%-
      include('./partials/headerA') %> <% } else { %> <%-
      include('./partials/headerU') %> <% } %> <% } else { %> <%-
      include('./partials/header') %> <% } %>
    </header>
    <div class="tabdiv">
    <% if ((loginS == true) && (loginP == true)) { %> 
    <% for (var i = 0; i < 20; i++) { %>
      <div class="room">
        <h2>Chambre : <%= temp[i].room %></h2>
        <% if (temp[i].state == "0") { %>
        <a
          class="btn btn-success"
          href="/occup/<%= temp[i].room %>"
          role="button"
          name="free"
          >Libre</a
        >
        <% } else {%>
        <a
          class="btn btn-danger"
          href="/elib/<%= temp[i].room %>"
          role="button"
          name="occupy"
          >Occupee</a
        >

        <% } %>
      </div>
      <% } %> 
      <% } else { %> 
    <% for (var i = 0; i < 20; i++) { %>
      <div class="room">
        <h2>Chambre : <%= temp[i].room %></h2>
        <% if (temp[i].state == "0") { %>
        <a
          class="btn btn-success disabled"
          href="#"
          role="button"
          aria-disabled="true"
          >Libre</a
        >
        <% } else {%>
        <a
          class="btn btn-danger disabled"
          href="#"
          role="button"
          aria-disabled="true"
          >Occupee</a
        >

        <% } %>
      </div>
      <% } %> 
      <% } %>
    </div>
    <footer id="footer"><% - include('./partials/footer') %></footer>
  </body>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
    crossorigin="anonymous"
  ></script>
</html>
